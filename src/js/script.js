// $(document).ready(function () {

// });

const slider = tns({
    container: '.feedback__slider',
    items: 3,
    speed: 700,
    slideBy: 1,
    nav: true,
    navPosition: "bottom",
    controls: false,
    autoHeight: true
});